# Doc 05F — Change Record (2026-09-17)

Paste as a new row in Doc 05F §24 Change Record, and apply the fifteen edits listed in the Formula Sheet §8 to the body.

| Version | Date | Change |
|---|---|---|
| 1.0 | 2026-09-17 | Reconciled with the Formula Sheet and with production. §11 replaced by a reference to `Doc_05F_formula_sheet.md` (that file is canonical for generation); `fallback_v1` added as the fail-open generator alongside `deterministic_v1`; `calendar_blocks.scope jsonb` replaces the `domain`/`skill_codes` columns, with practice split into one block per section carrying a domain mix; `PlanInput` gains `recent_planned_by_domain`, `engine_planning`, `exams.missed_count/reviewed`, `degraded[]`; validator V-04/V-06/V-08/V-10 amended; INV-08-06's stored 50-snapshot set replaced by the seeded 3,000-snapshot suite the parity gate regenerates. Corrections against production: mastery levels are **0–4** (`public.mastery_levels`, CHECK `0..4`), so `weight_by_level` re-keys to `{0:5,1:4,2:3,3:2,4:1}`; `calendar_runtime_config_history` is **per-table** (thirteen such tables exist), not shared; RLS uses `auth.uid()` and `profiles.role='admin'` because `current_student_id()`/`is_admin()` do not exist (closes G-08-08). Streak ownership moves out of the calendar: `/api/me/streak` reads `student_overall_kpi` (Doc 05B), and SCL-08-E now asks 05B for a student-local day boundary and the rest-day skip (absorbs G-08-11 and G-08-12). Launch `enabled_block_types = ["practice"]`; review and full-length are rebuild verticals whose adapters ship as fail-open stubs with contract tests (G-08-02, G-08-03). Review launch modes are `queue` and `session(source_engine, source_session_id)`; no `source_origin` field is required of the review engine. Guardian calendar reads use `/api/students/:studentId/calendar` through the existing subject-resolver pattern. |
