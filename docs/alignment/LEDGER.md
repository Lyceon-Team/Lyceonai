# Alignment Ledger

Append-only record of `spec-align` passes. Newest at the bottom. One row per pass.
Columns: Date · Pass · Scope · Spec basis · PR · Notes.

| Date | Pass | Scope | Spec basis | PR | Notes |
|------|------|-------|------------|----|-------|
| 2026-06-03 | Pass 0 | CI baseline reset (green pipeline) | Coding Standards §14 (CI/verification) + spec-align "Pass 0" | #325 | Root cause of "dead CI" was org-level disabling of GitHub-hosted runners (every run startup-failed, no runner) — re-enabled by Karl, not a workflow bug. Replaced the dead `ci`/CodeQL workflows with a verified-green required `ci` job (install → route:validate → test:security → test:ci → build → dist-check) + a slim non-blocking `ci-known-gaps` job (source typecheck + audit). **Deleted 8 stale CI contract test files wholesale** (Karl's call: clean delete + rebuild later; catalogued in `KNOWN-GAPS.md`). Node pinned via `.nvmrc` (22); pnpm 10. CodeQL advanced quarantined to manual-only; **admin must enable CodeQL default setup** + set branch-protection required check to `ci`. Deferred to Wave 0: `package.json` `engines.node`, npm-shelling typecheck script, ESLint. No app source / dependency / lockfile changes. |
| 2026-06-05 | Pass 1 / Wave 0 | Shared foundations (Node/pnpm SSOT, `@lyceon/shared` primitives, CI, ESLint, anti-leak skill) | Coding Standards §2, §4.1, §7.2, §7.3, §12.1, §14, §16, §17; Doc 02B §684 | _pending (draft → cleanup)_ | Branch `claude/align-wave-0` off `origin/cleanup`, **PR → cleanup** (branch-model directive). Node/pnpm SSOT trio completed: `engines.node="22"`, `packageManager="pnpm@10.33.0"`, CI derives pnpm from `packageManager` (no floating pin). pnpm-only scripts (de-npm'd `typecheck`/`verify:csrf`). `packages/shared` renamed `@sat-copilot/shared`→`@lyceon/shared` (zero importers) and established as SSOT **island, proven** (own `tsconfig` + vitest in CI, NOT in root tsconfig include): added Zod **env** (provisional), seeded **RNG** (`createRng`/`seededShuffle`/`deriveSelectionSeed`, Doc 02B §684), fixed `any` in `id.ts`. New canon gates BLOCKING in `ci` (check:shared/test:shared/lint:shared); ESLint stood up (flat config, §17 hard-stops at error) — BLOCKING for `packages/shared`, ADVISORY for legacy via `ci-known-gaps`. Anti-leak skill promoted → `.claude/skills/anti-leak/`. CI `push` → `[main, cleanup]`. Logger NOT touched — `server/logger.ts` is **canonical-for-now** (already redacts §12.1; `apps/api` delegates to it — not a fork). Verified green: test:ci 275, shared 18, build. |
| 2026-06-06 | Wave 1 / anti-leak | Exam pre-completion field-leak + practice serializer consolidation | Doc 04A §10.2; Doc 02B §20; Doc 04C §2.7 | _pending (draft → cleanup)_ | Branch `claude/align-anti-leak` off `origin/cleanup`. **#2:** exam pre-completion (active-section + pre-unlock review) strips `domain`/`skill_code`/`difficulty` (projection nulls + `GetCurrentSessionResult` type + allowlist; `SafeQuestionPreCompletion` types them literal-`null` so the unsafe shape is uncompilable); post-completion review re-adds them. **#3:** practice option-heal path consolidated onto the single `toStudentSafeQuestionDTO` serializer. Regression tests RED→GREEN. spec-auditor APPROVED the targeted #2/#3 fixes (zero blockers); the HIGH `score_runs` review-unlock gap (**finding #1**) was **consciously deferred** to the exam-scoring unit — no exploitable leak (synchronous scoring; `score_runs` pipeline unbuilt), documented in `KNOWN-GAPS.md` + inline `@gap`. Practice `difficulty` exposure noted as a separate pre-existing gap (§20/Preamble §12 do not authorize it). Verified: test:ci 275; exam projection tests green; tsc at pre-existing baseline; zero new lint violations. |

## Fast-follow backlog (tracked; do not let these evaporate)

Named follow-ups owed from Wave 0 and the drift audit. Each becomes its own scoped pass/PR.

| Item | Why | Owner / when |
|------|-----|--------------|
| **Logger/redaction relocation** | Move `server/logger.ts`'s `redactSensitive` + structured logger into `@lyceon/shared` and re-export, once the root↔workspace **esbuild `--packages=external`** bundling boundary is solved. No second logger may be created meanwhile — `server/logger.ts` is canonical-for-now. | Foundations fast-follow |
| **Bulk `@shared/*` → `@lyceon/shared` migration** | Standing rule from Wave 0: no **new** `@shared/*` imports; every wave migrates the imports of files it touches. The remaining bulk migration of the legacy top-level `shared/` is the tail. | Incremental + dedicated cleanup PR |
| **Env required-set reconciliation (Doc 06A/06B)** | `@lyceon/shared` env schema is **PROVISIONAL** (observed contract). HARD GATE: no consumer migrates onto it until the required-vs-optional set is reconciled against Doc 06A/06B (one targeted read). | Before first env consumer |
| **Lint graduation criterion** | ESLint is ADVISORY for the legacy tree. Graduates to fully BLOCKING when advisory violations reach zero — at the latest at hardening. Every wave lint-cleans the files it touches. | Ongoing → hardening |
| **`packages/shared` type/schema consolidation (§7.2)** | Pre-existing in the canon: `types.ts` hand-defines `CanonicalQuestion` / `AnswerKey` / `SectionCode` etc. that shadow the Zod schemas in `validate.ts`. §7.2 mandates schema-first + inferred types. Infer all from the Zod schemas and delete the hand-written shadows (bounded refactor; not done in Wave 0 to keep the pass small + green). | Foundations fast-follow |
| **`tutor-runtime` unit (NEW wave)** | `apps/api/src/lib/tutor-log.ts` persists tutor `message`+`answer` **verbatim** and `console.log`s — breaches "tutor conversations are ephemeral and non-verbatim" (CLAUDE.md privacy invariant; Doc 03 family). Slot **immediately after Wave 2** (Karl may resequence). | New wave, post-Wave-2 |
| **Exam-scoring unit (`score_runs` review-unlock gate)** | Doc 04C §2.5/§2.7 require review reveal to be gated on a successful `score_runs` row, but the async 04B/`score_runs`/04D pipeline is unbuilt; exam reveal currently gates on `session.status === 'completed'`. Safe today (synchronous scoring → no `scoring_pending` window) but **not spec-conformant**; flagged HIGH by the anti-leak auditor and deferred (Wave 1). Build 04B `score_runs` + 04C state derivation + 04D failure ledger, then replace the status gate. Detail in `KNOWN-GAPS.md`. | New wave (exam-scoring) |
