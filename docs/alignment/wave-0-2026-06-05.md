# Alignment Brief — Wave 0 (Shared Foundations) · 2026-06-05

**Pass:** Pass 1 = Wave 0 (serial, lands first). Run as `/spec-align` over the foundations unit.
**Branch:** `claude/align-wave-0` off `origin/cleanup`. **PR target: `cleanup`** (branch-model directive; overrides the skill template's `main`).
**Scope (one bounded unit):** shared foundations every later wave depends on — Node/pnpm SSOT, `packages/shared` canonical primitives (env validation, logger/redaction, seeded-RNG, core schemas), CI hardening, pnpm-only scripts, ESLint stand-up, anti-leak skill promotion.

## Spec basis (cited, read-only)
- **§2 Monorepo Layout** + **§7.2** — `packages/shared` is the single source of truth for Zod validators/types/helpers.
- **§7.3** — environment variables validated via Zod (`packages/shared/env.ts` pattern), parsed at startup.
- **§4.1** — determinism: selection stable/explainable; randomness only deterministic-per-session (seeded). Backs the seeded-RNG util (also Doc 02B §684's seeded Fisher-Yates for the determinism wave).
- **§12.1** — never log secrets/cookies/tokens/student answers/tutor payloads; structured + redact by default. Backs the logger/redaction SSOT.
- **§14** + **§16** — `pnpm` only (never `npm`); ESLint + `@typescript-eslint`; no `console.log` in product code (use the structured logger).
- **§17** — hard stops (no `any`, no silent catch, no client-trusted role, no answer leak, no vanity metrics, no duplicate types shadowing a Zod schema).

## Pre-flight (Q1) — PASSED
- `f20f74b` (Pass 0) is an ancestor of `origin/cleanup` (merged via PR #331). `cleanup` carries Pass-0 `ci.yml` (`10d502d`) + `.nvmrc`(22).
- CI trigger wedge: **not present** — `on.pull_request:` is unfiltered, so PRs into `cleanup` report the required `ci` check. `push:` was `main`-only → broadened to `[main, cleanup]` for first-class integration-branch CI.

## Landed this pass (verified green: route:validate ✅ · test:ci 275 ✅ · test:security 6 ✅)
- **Node/pnpm SSOT trio completed (Q2 = pnpm@10 + node 22):** `package.json` `engines.node="22"`, `packageManager="pnpm@10.33.0"`; CI `Setup pnpm` no longer pins `version` — pnpm is **derived from `packageManager`** (single source). `.nvmrc`(22) already governs node in CI + dev.
- **pnpm-only scripts (§14/§16):** `"typecheck": "npm run check"` → `pnpm run check`; `"verify:csrf": "npx tsx …"` → `pnpm exec tsx …`.
- **CI hardening:** `push.branches` → `[main, cleanup]`.
- **Anti-leak skill promoted:** `skill-drafts/anti-leak/` → `.claude/skills/anti-leak/` (SKILL.md + `evals/evals.json` + `references/reveal-matrix.md`; iter-1 benchmark 100% with-skill). Eval workspace stays in `skill-drafts/`.

## Open decisions — gated; surfaced as numbered questions (do not guess)
1. **`packages/shared` activation depth** — island establish-only (rename + add primitives, re-link via `pnpm install`, wire into tsconfig/build/test later as consumers arrive) vs full activation now (add `packages/**` to tsconfig include + build + tests; may surface new typecheck). Note: `tsconfig.json` include currently omits `packages/**`, so `packages/shared` is an unchecked, unconsumed island today.
2. **Env-validation fold-in grounding** — define the canonical Zod env from the observed contract (`.env.example` + `apps/api/src/env.ts`, which is ad-hoc/non-Zod) with core Supabase/`NODE_ENV`/`CSRF_SECRET` required, providers optional, required-set confirmed vs Doc 06A/06B during migration — vs read Doc 06A/06B now to fix the authoritative required-set.
3. **Logger/redaction unification mode** — extract `redactSensitive`+patterns from the existing 505-line `server/logger.ts` into `@lyceon/shared` and re-export (requires wiring the **root** package to consume the workspace pkg; esbuild `--packages=external` bundling risk) — vs defer to a tracked fast-follow and create **no** second redact copy now (avoids divergence + build risk). `server/logger.ts` already redacts per §12.1; `apps/api/.../logging.ts` already delegates to it (not a true fork). `apps/api/src/lib/tutor-log.ts` stores tutor message+answer verbatim + `console.log`s — **privacy-invariant drift for the tutor-runtime wave, out of Wave-0 scope** (flagged).
4. **ESLint stand-up** — adds devDeps (lockfile change) and the legacy tree will have many violations: config + `pnpm lint` script run **advisory/non-blocking** in `ci-known-gaps`, tighten incrementally — vs blocking-but-scoped to clean code — vs defer to its own pass.

## Hard rule from Wave 0 onward (Q4)
No **new** `@shared/*` imports anywhere; every later wave migrates the `@shared/*` imports of files it touches into `@lyceon/shared`. The remaining bulk migration is a tracked fast-follow with its own ledger row, not an open tail.

## Cross-references discovered (not Wave-0 work; tracked for owning waves)
- Anti-leak: exam review reveal gated on `status==='completed'` not `score_runs` (Doc 04C §2.7) — **HIGH**, Wave 1.
- Determinism: `questions-runtime.ts:157` `Math.random()` selection; `practice-canonical.ts:364` unseeded `fisherYates` (Doc 02B §684) — **HIGH**, Wave 2 (consumes the Wave-0 seeded-RNG util).
- Mastery/KPI: forbidden `confidence_metric`/`readiness_metric`/`consistency_metric`, `M_INIT=50`, raw-accuracy projections (Doc 05A–05D) — **HIGH**, Wave 3. Directive: formulas live in Supabase PL/pgSQL + DB constants, TS is a thin read/serve surface.

## Rulings applied (Stage 3) — Q1–Q4 RESOLVED
- **Q1a (amended):** island established + **proven** — `@lyceon/shared` has its own `tsconfig` + vitest wired into CI; root tsconfig include untouched. Determinism + env tests are mandatory and present (18 tests). Root build wiring deferred to consumers.
- **Q2b:** pnpm@10 + node 22; `packageManager="pnpm@10.33.0"`; CI derives pnpm from it.
- **Q2a (amended):** env schema is **PROVISIONAL** (header-annotated); HARD GATE — no consumer migrates until Doc 06A/06B reconciliation (ledger fast-follow).
- **Q3a (confirmed):** logger NOT relocated; `server/logger.ts` canonical-for-now (no second copy); relocation is a ledger fast-follow. `tutor-log.ts` verbatim-persistence → new **tutor-runtime** unit (ledger), slot after Wave 2.
- **Q4 (hybrid):** ESLint stood up; §17 hard-stops at error; BLOCKING for `packages/shared`, ADVISORY for legacy; graduation criterion in ledger; devDeps approved + added.

**Stage-3 gate:** green — `pnpm install --frozen-lockfile` in sync; route:validate, test:security, test:ci (275), check:shared, test:shared (18), lint:shared, build all pass.
