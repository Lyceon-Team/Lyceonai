$kind: http-request
url: "{{baseUrl}}/api/auth/admin-provision"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "{{adminEmail}}",
      "password": "{{adminPassword}}",
      "passcode": "{{adminPasscode}}"
    }
order: 10000
