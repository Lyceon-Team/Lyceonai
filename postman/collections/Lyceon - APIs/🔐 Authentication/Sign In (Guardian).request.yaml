$kind: http-request
url: "{{baseUrl}}/api/auth/signin"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "email": "{{guardianEmail}}",
      "password": "{{guardianPassword}}"
    }
order: 5000
