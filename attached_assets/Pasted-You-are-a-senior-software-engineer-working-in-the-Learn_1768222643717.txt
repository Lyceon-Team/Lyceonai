You are a senior software engineer working in the Learningai monorepo for lyceon.ai.

Objective (measurable)
Make practice questions render end-to-end in production with a stable contract aligned to Supabase tables.

This requires:
1) /api/practice/next returns a valid question payload for an authenticated user (HTTP 200)
2) The practice page (/practice/math) renders a question with options reliably
3) KPIs failures MUST NOT block practice rendering (practice still works even if /api/progress/kpis fails)
4) Add a deterministic /api/health/practice endpoint that detects schema/credential mismatches before users do.

Source of Truth: Questions table schema
The `questions` table in Supabase matches the attached CSV `questions_rows (6).csv`.

Confirmed columns that exist (must use):
- id
- section (e.g. "Math")
- stem
- type (e.g. "mc")
- options (stored as STRING containing JSON array)
- answer_choice (e.g. "B")
- answer_text (e.g. "$23.20")
- explanation
- difficulty
- classification
and many others, but DO NOT assume any missing fields exist.

Confirmed columns that do NOT exist (must NOT select):
- correct_answer
- passage
- passage_id

Non-negotiable Guardrails
- Do NOT refactor architecture or rename routes.
- Do NOT add new packages.
- Limit code changes to the smallest set of files required.
- Every claim must be backed by grep/curl proofs and exact file excerpts.
- Absolutely no “it should work” — only “it works” with evidence.

In-scope files (primary)
1) server/routes/practice-canonical.ts  (practice API spine)
2) client/src/hooks/use-adaptive-practice.ts (make KPIs non-blocking)
3) apps/api/src/routes/progress.ts (ONLY if necessary; prefer client-side non-blocking)
4) server/routes/supabase-auth-routes.ts (add auth header support for debug endpoint)
5) NEW: server/routes/health-routes.ts (add /api/health/practice)

Phase 0: Confirm what prod is doing (Evidence Required)
Problem: curl to /api/auth/debug returned hasAccessCookie=false, because curl doesn’t include browser cookies.
We need deterministic server-side validation in a way curl can exercise.

Action A: Update /api/auth/debug so it accepts an Authorization header
- If Cookie access token is absent, allow:
  Authorization: Bearer <token>
- Resolve userId from either cookie OR bearer token.
- Return:
  session.resolvedUserId
  serviceRole.canReadUser
  serviceRole.userLookupError

Evidence required:
- Paste updated snippet from server/routes/supabase-auth-routes.ts showing bearer token parsing.
- Provide two curl commands:
  1) curl without auth → resolvedUserId null
  2) curl with bearer token → resolvedUserId populated, serviceRole.canReadUser boolean

Phase 1: Lock a stable PracticeQuestionVM contract (Deterministic)
Define a minimal response shape returned by /api/practice/next:

PracticeQuestionVM:
{
  id: string,
  section: string,
  type: 'mc' | 'fr',
  stem: string,
  options: { key: string, text: string }[], // ALWAYS ARRAY
  difficulty: string | null,
  classification: any | null,
}

Rules:
- Never include columns that don’t exist in DB.
- Never return options as a string.
- Do not leak correct answers in the question payload unless the UI absolutely needs it (prefer to only return correctness after submission).

Implementation:
In server/routes/practice-canonical.ts for GET /next:
- Select only columns that exist:
  id, section, stem, type, options, difficulty, classification
  + (optionally) question_type if needed, but prefer `type`.
- Parse options robustly:
  let options = row.options ?? [];
  if (typeof options === 'string') { try { options = JSON.parse(options); } catch { options = []; } }
  if (!Array.isArray(options)) options = [];
- Return PracticeQuestionVM

Evidence required:
- Paste the exact `.select(...)` string used.
- Paste the options parsing block.
- Provide curl proof of HTTP 200 and options as JSON array.

Phase 2: Make session creation insert fully schema-aligned (Deterministic)
Current failures we’ve seen include FK failures on practice_sessions.user_id and status constraints.

Rules:
- practice_sessions.status MUST be one of allowed values (use 'in_progress' on create).
- practice_sessions.user_id MUST equal the authenticated Supabase user id.
- If serviceRole.canReadUser is false, return a clear JSON error indicating misconfigured SUPABASE_SERVICE_ROLE_KEY / project mismatch.

Implementation:
In practice-canonical.ts:
- On session create: status='in_progress'
- Ensure userId is derived from verified auth and not from any untrusted input.
- If insert fails:
  - return { error: 'session_create_failed', detail: <supabase error message>, requestId }
  - include the Supabase error code/message verbatim.

Evidence required:
- Paste the session insert payload code block.
- Show prod curl for /api/practice/next with bearer auth that returns 200.
- If it fails, paste exact JSON response error.

Phase 3: Answer submission correctness aligned to real columns (Deterministic)
Since `correct_answer` doesn’t exist:
- For MC: correct key is answer_choice.
- Only compute correctness on /answer.
- /answer must select: answer_choice, answer_text, explanation.
- Compare:
  isCorrect = selectedAnswer === answer_choice
  (For free response, compare normalized text; but if FR isn’t required for launch, ship MC-only correctness deterministically and explicitly.)

Evidence required:
- Paste the correctness computation code.
- Provide curl that submits an answer and returns isCorrect boolean.

Phase 4: KPIs must NOT block practice (Big swing for stability)
Today, /api/progress/kpis can 500 and break the page.
We must decouple it so practice can still render questions.

Preferred approach (fastest, least risky):
Update client/src/hooks/use-adaptive-practice.ts:
- When fetching KPIs, wrap in try/catch.
- If KPIs fails, set default KPI object and continue.
- Practice rendering must proceed if /api/practice/next succeeds.

Do NOT make practice page depend on KPIs response.

Evidence required:
- Show the client-side change where KPI fetch failure does not throw.
- Browser proof: practice page shows question even if /api/progress/kpis returns 500.

Phase 5: Add /api/health/practice endpoint (Prevent future drift)
Create a new endpoint: GET /api/health/practice
This endpoint should be service-role only (or allow unauthenticated but safe output). It must:
1) Verify environment keys are set (supabaseUrlSet, serviceKeySet) WITHOUT printing secrets
2) Verify service role can access auth and read a user by id:
   - If Authorization bearer token provided, resolve userId then:
     admin.auth.admin.getUserById(userId) must succeed
3) Verify required DB columns exist using information_schema queries:
   - questions: id, section, stem, type, options, difficulty, classification, answer_choice, answer_text, explanation
   - practice_sessions: id, user_id, section, status, started_at, finished_at (or confirm actual column used)
   - answer_attempts: id, session_id, question_id, selected_answer, is_correct, attempted_at (and user_id is optional)
4) Return a JSON report:
{
  ok: boolean,
  auth: { resolvedUserId, serviceRoleCanReadUser },
  schema: {
    questions: { hasColumns: [...], missingColumns: [...] },
    practice_sessions: { ... },
    answer_attempts: { ... }
  }
}

If ANY required column missing → ok=false and list missingColumns.

Evidence required:
- Paste the endpoint implementation file path and code snippet of the information_schema query.
- Provide curl output from production:
  curl -s -H "Authorization: Bearer <token>" https://lyceon.ai/api/health/practice | jq
  ok must be true.

Final Proof Bundle (Required)
Before saying “done”, provide:
1) Changed files list
2) Grep proofs:
   - no references to correct_answer/passage/passage_id in practice-canonical
3) Curl proofs (prod):
   - /api/practice/next (Bearer auth) → 200 and options is array
   - /api/practice/answer (Bearer auth) → 200 and isCorrect boolean
   - /api/health/practice (Bearer auth) → ok:true
4) Browser proof: /practice/math shows a question with visible options even if /api/progress/kpis fails

DO NOT expand scope beyond practice stability.
DO NOT touch Stripe in this task.
