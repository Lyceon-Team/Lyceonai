node_modules/
.env
.env.*.local
dist/
client/dist/
*.log
.DS_Store
.vscode/settings.json
.replit
replit.nix
.cache
.upm
*.backup
*.bak
package-lock.json
*.tsbuildinfo

# Data exports - prevent leaking question answers
*.csv
!docs/samples/*.csv

# Audit outputs
audit-output.json
test-results/

# Playwright e2e harness outputs (browsers install to /opt/pw-browsers, outside the repo)
playwright-report/
blob-report/
.playwright/

# Local secrets
.env.local
.env
.env.*
!.env.example

.vercel

# skill-creator eval artifacts (not part of the repo)
skill-drafts/*-workspace/

# Python bytecode (mastery reference + CI parity driver)
__pycache__/
*.pyc
tests/__fixtures__/assemble-batch-scratch/
tests/__fixtures__/assemble-batch-apply/

# AUD-519-007: generated file is now COMMITTED so Cloud Run --source . has it.
# The prebuild script refreshes it from shared/ for local dev; CI drift-check
# verifies the committed copy matches the source of truth in shared/.
# apps/workers/tutor-orchestrator/src/lib/_tutor-orchestrator-wire.generated.ts
.claude/worktrees/
