name: CI

on:
  pull_request:
  push:
    branches: [main, cleanup]

# CI runs without secrets by default (for forks and basic validation).
# Secrets are only required for integration tests, which are skipped here.
env:
  CI: true

jobs:
  # ---------------------------------------------------------------------------
  # Required gate. Every step here is verified GREEN against the current tree
  # (Pass 0, 2026-06-03). Node version comes from .nvmrc (single source of
  # truth shared with local dev); pnpm matches the version used to verify.
  # ---------------------------------------------------------------------------
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        # No version pin here: pnpm version is derived from package.json
        # "packageManager" (pnpm@10.33.0) — single source of truth (Q2).

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate Route Registry
        run: pnpm run route:validate

      # ZERO NESTED ANCHORS (issue #829). Sixteen files carried the wouter v2 idiom —
      # a bare `<a>` child inside `<Link>` — which under wouter 3 renders `<a href><a
      # data-testid>`, leaving the inner anchor (the one with the testid, the label and
      # the click target) with no href at all. Middle-click, ⌘-click, open-in-new-tab,
      # copy-link and screen-reader link announcement were dead on every tab in the
      # product; a plain left click worked, which is why nobody noticed.
      #
      # `--selftest` runs FIRST and on its own line. A gate whose own matcher has rotted
      # reports zero occurrences and reads exactly like a clean tree, so the fixtures are
      # exercised before the scan is believed. The `asChild` exemption is by SHAPE, never
      # by filename — see the script's header for why that distinction is load-bearing.
      - name: "No nested anchors (issue #829)"
        run: |
          set -euo pipefail
          node scripts/ci/nested-anchor-gate.mjs --selftest
          node scripts/ci/nested-anchor-gate.mjs

      # No SCL id may head two entries in the register. Three collisions
      # (SCL-021, SCL-024, SCL-042) reached it despite every author having read
      # the register first — reading does not reserve a number, so only a
      # mechanical check closes the same-day race. The two pre-existing
      # collisions are allowlisted by exact count inside the script, with an
      # expiry note; a third heading on either still fails.
      # §17.1's band is Doc 05C's, summed. The selftest runs FIRST and on its own line: a
      # gate whose matcher has rotted reports zero findings and reads exactly like a clean
      # tree.
      - name: Calendar projection is read, never recomputed
        run: |
          set -euo pipefail
          node scripts/ci/calendar-projection-gate.mjs --selftest
          node scripts/ci/calendar-projection-gate.mjs

      - name: SCL register — no duplicate ids
        run: node scripts/ci/scl-duplicate-check.mjs

      - name: Run Security Tests (IDOR + entitlements regression)
        run: pnpm run test:security

      # Full deterministic CI suite. The stale-mock contract files were deleted
      # in Pass 0 (clean delete + rebuild later — see docs/alignment/KNOWN-GAPS.md),
      # so this runs green with no exclusions.
      # WHOLE SUITE, BLOCKING. This step used to run `test:ci` = `vitest run
      # tests/ci`, a fraction of the repo's tests. The rest were reported for
      # weeks as "pre-existing, gated" while running in no job at all — path
      # scope is a worse suppression than an accept-list, because it leaves no
      # record and the files still read as coverage.
      #
      # EXACT ARITHMETIC, re-measured at this head (the earlier comment said
      # 182/170 and was stale by one file after the dead test was deleted):
      #
      #   181 tracked *.{test,spec}.{ts,tsx}
      #   = 169 vitest-selected
      #   +   9 Playwright .spec.ts   — match no vitest `include`, run in NO job
      #   +   2 tests/integration/**  — excluded; the `integration` job owns them
      #   +   1 tests/auth.integration.test.ts — excluded as "moved to
      #                                  tests/integration/", but the original is
      #                                  still tracked. A stale duplicate.
      #
      # REPORT SELECTED AND PASSED SEPARATELY. A green run reads
      # `161 passed | 8 skipped (169)` — 169 selected, 161 passed, 8 skipped.
      # Quoting the passed count as though it were the selected count is how a
      # skip becomes invisible, which is the same class of error as counting a
      # file that never ran as coverage.
      #
      # Cost measured on the RUNNER, not locally: 17.3s -> 54.3s. The PR body
      # originally said +25s from local timing; the hosted figure is +37s. Still
      # well inside the budget for a job that was ~45s, so no fast/full split is
      # proposed — but the number quoted is the one from the machine that
      # actually gates merges.
      - name: Run Tests (whole suite — blocking)
        run: pnpm test

      # Wave 0 canonical primitives (@lyceon/shared): the new SSOT is born proven —
      # strict typecheck, its own tests, and lint at error level all BLOCKING.
      - name: Shared package — typecheck (blocking)
        run: pnpm run check:shared

      - name: Shared package — tests (blocking)
        run: pnpm run test:shared

      - name: Shared package — lint (blocking)
        run: pnpm run lint:shared

      # build:vercel is a superset of build: it runs `build` and then emits
      # dist/vercel-api.cjs, the CJS bundle Vercel actually loads. CI used to
      # build only dist/index.js, so the artifact production runs was never
      # compiled here — which is part of why a bundle that could not start was
      # able to pass CI on 2026-08-27.
      - name: Build (including the Vercel production bundle)
        run: pnpm run build:vercel

      - name: Boot probe (production bundle must complete module load)
        run: pnpm run probe:boot

      - name: Boot probe self-test (proves each failure mode turns it red)
        run: bash scripts/ci/boot-probe.selftest.sh

      # @spec [Codex audit Finding 4] The worker's tsconfig uses NodeNext, which
      # rejects extensionless relative imports (TS2835). The monorepo root build
      # does NOT compile apps/workers/, so a broken import passes CI until
      # Cloud Build fails in production. This step runs the isolated worker
      # typecheck so extensionless imports are caught here, not in deploy.
      - name: Worker isolated typecheck (NodeNext — extensionless imports gate)
        run: cd apps/workers/tutor-orchestrator && pnpm exec tsc -p tsconfig.json --noEmit

      - name: Check No Test Routes in Dist
        run: node scripts/ci/check-no-test-routes-dist.mjs

      - name: No hardcoded constants (Lane-A gate — DB is source of truth)
        run: node scripts/ci/no-hardcoded-constants.mjs

      - name: Tutor never writes mastery (C-7 / INV-03-01)
        run: node scripts/ci/tutor-never-writes-mastery.mjs

      # SAT_TAXONOMY shipped slugs ('math', 'advanced_math', 'linear_equations')
      # against a database holding 'M', 'Advanced Math', 'Linear Equations in One
      # Variable'. Nothing joined and the mastery page said "No Mastery Data Yet" to
      # every student who had data. It survived because the TESTS seeded the same
      # slugs: the suite agreed with the broken code instead of with the schema.
      - name: Test fixture canonicality gate (section/domain must match the DB CHECKs)
        run: node scripts/ci/test-fixture-canonicality-gate.mjs

      - name: Fixture canonicality self-test (proves each mutation turns it red)
        run: bash scripts/ci/test-fixture-canonicality-gate.selftest.sh

      # Five section vocabularies coexisted here: 'M'/'RW' (what all sixteen CHECK
      # constraints accept), 'MATH'/'RW', 'Math'/'Reading & Writing', 'math'/'rw', and
      # 'Math'/'RW'/'Random'. The last one reached storage — jsonb, inside
      # practice_sessions.filters, the one section field no CHECK can see — and thirteen
      # production rows held it. This gate keeps the count at one.
      - name: Section vocabulary gate (no section literal outside M/RW off the display path)
        run: node scripts/ci/section-vocabulary-gate.mjs

      - name: Section vocabulary self-test (proves each mutation turns it red)
        run: bash scripts/ci/section-vocabulary-gate.mutations.sh

      # Deleting a route removes the server half; a forgotten caller fails at RUNTIME as a
      # 404 the UI renders as an error state or an empty list. The path is a string, so tsc
      # cannot see it — only a text search over the committed tree can.
      # R4's review UI shares practice's question loop through an engine config, so a
      # careless edit to the loop can silently break one engine while the other's tests
      # stay green. This gate plants ten mutations in PRODUCT SOURCE — a wrong endpoint,
      # a pre-submit reveal, a session id kept out of the URL, a missing entry mode, a
      # UTC day header, an error state for an empty pool, both halves of "abandoned
      # sessions never appear", a removed nav entry, a removed allowlist entry — and
      # fails unless each one turns its own U-test red and reverts byte-identically.
      - name: Review UI mutation gate (proves U1-U9 are load-bearing)
        run: bash scripts/ci/review-ui-gate.mutations.sh

      - name: Retired endpoints gate (no caller of a deleted route may remain)
        run: node scripts/ci/retired-endpoints-gate.mjs

      - name: Retired endpoints self-test (proves each mutation turns it red)
        run: bash scripts/ci/retired-endpoints-gate.selftest.sh

      # "Every subject-scoped handler is role-blind" is an ALL-ROUTES property, and any
      # all-routes property is a chokepoint: enforced once, never per route. Three privilege
      # divergences in this vertical were each found by collapsing two independently-written
      # paths; while two paths serve one resource a fourth instance is a schedule, not a risk.
      - name: Subject-resolver chokepoint gate (no handler below the resolver learns the caller's role)
        run: node scripts/ci/subject-resolver-chokepoint-gate.mjs

      # The consent surface was written against columns that do not exist and CI stayed green
      # for months, because the tests mocked Supabase and returned fixtures spelling the same
      # imagined names. Code and mock agreed; neither was ever compared to Postgres. This
      # forbids both halves on guardian paths — a query-layer mock with no real pg.Client
      # behind it, and a hand-written row literal.
      - name: Guardian schema-truth gate (no mocked query layer, no hand-written rows)
        run: node scripts/ci/guardian-schema-truth-gate.mjs

      - name: Guardian schema-truth self-test (proves each rule turns it red)
        run: bash scripts/ci/guardian-schema-truth-gate.selftest.sh

      # A published legal version directory is the evidence of what somebody
      # agreed to — ARL § 17602(a)(4) requires producing that exact text three
      # years later. Editing one destroys the evidence while the consent record
      # still names it. The gate asks git, not the content_hash sitting beside
      # the file: an author who edits en.md and updates the hash in the same
      # commit defeats a self-referential check, and that is the shape the
      # mistake actually takes. Case (B) of the self-test is exactly that.
      # The immutability gate compares against the base branch, and the
      # default checkout is shallow with no remote-tracking ref for it. Fetch
      # exactly the one commit it needs. If this fetch fails the gate exits 2
      # and says so — it will not pass without a comparison point.
      - name: Fetch base ref for the legal immutability gate
        run: |
          # `github.base_ref` is empty on a push event, so the fallback is what a push to an
          # integration branch actually uses. It must name a branch that cannot disappear:
          # the previous value, `stripe`, was deleted after merging and took this gate's
          # comparison point with it, turning the push build red on every integration branch.
          BASE="${{ github.base_ref || 'main' }}"
          git fetch --no-tags --depth=1 origin \
            "+refs/heads/$BASE:refs/remotes/origin/$BASE" || true

      - name: Legal versioning gates (immutability, manifest, cross-reference, body purity)
        run: pnpm run gate:legal

      - name: Legal gates self-test (proves each rule turns them red)
        run: pnpm run gate:legal:selftest

      # 2026-09-16 incident: the SERVER started resolving legal metadata from
      # `legal/` at request time, the Vercel function has no such tree, and every
      # consent lookup threw — /api/profile 500 and sign-in down. The renderer's
      # path had a gate; the server's did not. This asserts the generated
      # registry is current AND that the built function bundle really carries it.
      - name: Legal registry reaches every runtime (incident 2026-09-16)
        run: pnpm run gate:legal-registry

      # practice_sessions.mode decides event_source_kind, so accepting it as free
      # text let a client classify its own activity in the mastery record AND
      # create a diagnostic session through the practice route, bypassing the
      # once-only guard. Integrity bypass, not a lifecycle bug.
      - name: Session-mode boundary gate (BUG-3 — client cannot name its own mode)
        run: node scripts/ci/session-mode-boundary-gate.mjs

      # The RULE is unit-tested in packages/shared; this covers the seam the unit
      # test cannot see — narrowing the route's status filter back to
      # ['created','active'] means the rule never receives the completed row and a
      # second diagnostic is startable again, with every unit test still green.
      - name: Diagnostic once-only gate (BUG-2 — no retake)
        run: node scripts/ci/diagnostic-once-only-gate.mjs

      # The diagnostic lifecycle is declared in three places that cannot import
      # from each other: the CASE arms in the migration, DIAGNOSTIC_STATES in
      # packages/shared, and the EstimateStatus union in the client (which has no
      # module path to packages/shared). A state the SQL emits and the TypeScript
      # cannot name degrades the student silently; a status the server emits and
      # the client cannot name renders a blank card, not a type error.
      - name: Diagnostic state drift gate (SQL <-> shared <-> client)
        run: node scripts/ci/diagnostic-state-drift-gate.mjs

      - name: Guards proven by planted violations (B-WS3-1 §E)
        run: bash scripts/ci/guards-selftest.sh

      - name: Practice schema-types gate (STRUCTURAL 1 — no phantom columns)
        run: bash scripts/ci/practice-schema-types-gate.sh

      - name: Wire-contract drift gate (AUD-519-007 — L1.4 single-contract)
        run: diff shared/tutor-orchestrator-wire.ts apps/workers/tutor-orchestrator/src/lib/_tutor-orchestrator-wire.generated.ts

      - name: Safety-constants drift gate (AUD-B1.1c-010 — anti-leak + boundary mirrors)
        run: diff shared/tutor-safety-constants.ts apps/workers/tutor-orchestrator/src/lib/_tutor-safety-constants.generated.ts

      # Mastery emission failed 100% of the time for seven weeks unnoticed. These two
      # gates block the two defects that made that invisible: console.* on mastery
      # paths, and a miscalled structured logger that put the data object into `event`
      # (which no log filter can match).
      - name: Mastery emission observability gate (M-05 / M-07)
        run: node scripts/ci/mastery-emission-observability.mjs

      # The destructive rehearsal scripts seed fabricated mastery rows and take PGHOST
      # from the environment. Six such rows were found unattributed in prod.
      - name: Destructive-rehearsal target guard — negative control (M-10)
        run: bash scripts/ci/rehearsal-host-guard-negative-control.sh

  # ---------------------------------------------------------------------------
  # Visibility job — NOT required for merge. Steps are continue-on-error so the
  # job stays green, but their failures remain visible in the logs/annotations
  # instead of being hidden. These are the remaining non-deletable gaps on main
  # (real source type errors + advisory-DB-dependent audit; see
  # docs/alignment/KNOWN-GAPS.md). Each is promoted into the required `ci` job
  # by the domain pass that fixes it. Nothing here is hidden or faked green.
  # ---------------------------------------------------------------------------
  # The failures in this job are now ACCEPTED with an owner and an expiry rather
  # than "known" in perpetuity — see ci/known-gaps.yaml. The JOB ID is left as
  # `ci-known-gaps` on purpose: renaming it would silently break any
  # branch-protection required-check that names it, and a merge-blocking config
  # change is not something to slip into a cleanup PR. Rename proposed as an
  # owner question in the PR instead.
  ci-known-gaps:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Full history required for the ratchet check (CI-GATING-003):
          # git merge-base needs common ancestry with the PR base branch.
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        # No version pin here: pnpm version is derived from package.json
        # "packageManager" (pnpm@10.33.0) — single source of truth (Q2).

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # BLOCKING, and deliberately first. Everything below may fail without
      # failing this job; this step is what keeps that permission finite. It
      # rejects an entry missing owner/expiry/reason/re_arm/command, and turns
      # red once an entry's expiry passes — so a suppression has to be renewed
      # by a human decision or the check goes back to blocking.
      - name: Accept-list gate (owner + expiry required) — BLOCKING
        run: node scripts/ci/known-gaps-gate.mjs

      - name: Accept-list gate self-test (proves it can refuse)
        run: bash scripts/ci/known-gaps-gate.self-test.sh

      # ACCEPTED under ci/known-gaps.yaml -> typecheck-ci-tsconfig, owner karl,
      # expires 2026-10-01. The gate step above fails this job once that date
      # passes. The previous comment here claimed "2 real type errors (exam
      # section field drift)"; the real count is 41 across 6 files — which is
      # what an unrenewed suppression decays into.
      - name: TypeScript Check (CI tsconfig) — accepted, expires 2026-10-01
        continue-on-error: true
        run: pnpm exec tsc -p tsconfig.ci.json

      # PROMOTED TO BLOCKING 2026-08-19. Measured before promoting: `pnpm run
      # audit:ci` reports ZERO advisories of any severity, so the
      # continue-on-error was suppressing nothing at all — it only guaranteed
      # that the first real high/critical advisory would land silently. There is
      # no accept-list entry for this because there is nothing to accept.
      - name: Security Audit (high/critical) — blocking
        run: |
          pnpm run audit:ci
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('audit-output.json','utf8'));
            const v = data?.vulnerabilities || {};
            const bad = Object.values(v).some(x => ['high','critical'].includes(String(x?.severity || '').toLowerCase()));
            if (bad) { console.error('High/Critical vulnerabilities found.'); process.exit(1); }
            console.log('No High/Critical vulnerabilities found.');
          "

      # ACCEPTED under ci/known-gaps.yaml -> eslint-legacy-tree, owner karl,
      # expires 2026-11-01. 2,368 problems; 96% are three rules, and 449 of those
      # (no-undef on a TypeScript tree) are a config defect rather than findings.
      # The standing rule — every wave lint-cleans the files it touches — is
      # recorded as the re-arm criterion in the entry, where it expires.
      # packages/shared lints BLOCKING in the required `ci` job.
      - name: ESLint (legacy tree) — accepted, expires 2026-11-01
        continue-on-error: true
        run: pnpm run lint

      - name: Summary
        run: |
          echo "::notice::Accepted failures are declared in ci/known-gaps.yaml — each with an owner, an expiry and a re-arm criterion."
          echo "::notice::The accept-list gate is BLOCKING and fails this job when an entry expires. Security audit was promoted to blocking 2026-08-19 (zero advisories)."

  # Optional integration tests — only on push to main, when secrets exist.
  # Not required for PR merge.
  integration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        # No version pin here: pnpm version is derived from package.json
        # "packageManager" (pnpm@10.33.0) — single source of truth (Q2).

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # THIS JOB HAD NEVER PASSED. It ran `vitest run tests/integration` against the
      # DEFAULT config, which excludes `tests/integration/**` so the unit suite stays
      # hermetic -- so the filter selected a directory the config had already excluded and
      # vitest exited 1 with "No test files found" on every push to main. Both tests are
      # real and all three secrets were already wired; nothing ran them.
      #
      # `test:integration` now names `vitest.integration.config.ts`, which does not carry
      # that exclusion. The summary gate is the other half: these tests `describe.skipIf`
      # themselves when a Supabase secret is absent, so without it a missing secret would
      # turn the job from loudly broken into silently green -- testing nothing and saying
      # so to nobody. If it goes red naming skipped tests, a secret is missing, and the
      # message says which.
      - name: Run Integration Tests
        run: |
          set -euo pipefail
          pnpm exec vitest run --config vitest.integration.config.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-integration.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-integration.json \
            --file tests/integration
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

  # ---------------------------------------------------------------------------
  # Genesis fresh-apply gate (WS-1 re-cut, Doc 00 §10 Phase 6). Proves the
  # supabase/migrations pipeline applies from scratch deterministically, matches
  # the committed expected schema, ships RLS enabled everywhere, keeps profiles.id
  # FK RESTRICT, and holds the anti-leak hard gate. No prod creds — throwaway PG.
  # ---------------------------------------------------------------------------
  genesis-fresh-apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16   # owned `vector` extension must be installable (genesis declares it)
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Genesis fresh-apply + determinism + anti-leak gate
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/genesis-fresh-apply.sh

      # The guardian gate has never executed in production: guardian_links holds zero rows,
      # and the TypeScript that would have written one addressed columns that exist in no
      # spec, no schema, and no prod table. Every guardian test to date mocked the link layer
      # away, which is how that survived ten weeks. This gate inserts REAL rows.
      - name: guardian_view_decision gate (ONE derivation, on real guardian_links rows)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/guardian-view-decision-gate.sh

      # Doc 04B V4.3 §7.2/§8.1/§8.2/§8.4 — every scoring-catalogue constraint is made
      # to FIRE on a fresh apply: the INSERT/UPDATE/DELETE seal on an active version's
      # constants, the attestation CHECK, the no-downgrade trigger, the (version, key,
      # section) unique index, the raise-on-miss helper, and the partial unique index
      # under a real two-session race (the friendly trigger cannot see the other
      # transaction, so only the index can refuse it). Each check asserts its own
      # SQLSTATE and constraint/message and must print its own `ok` line.
      # @spec [Doc-04B_V4.3, §7.2, §8.1, §8.2, §8.4, Appendix A] | @implemented [2026-09-23]
      - name: Scoring catalogue gates (04B §7.2/§8 constraints observed firing)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/scoring-catalogue-gates.sh

      # Doc 04A V2.2 §5/§6/§9 + Doc 04B §13 as amended (owner ruling 2026-09-24,
      # SCL-121) — every exam-runtime constraint is made to FIRE on a fresh apply,
      # against synthetic fixtures: published-form and published-item immutability,
      # the one-way status machine, module2_path lock, the publish gate (since E4
      # v1.0 is active: publish succeeds against it and is refused for a candidate
      # or superseded version), the ruled
      # per-module composition tallies (difficulty / grid-in / domain, each miss
      # naming its cell), form-item uniqueness, answer-upsert idempotency, and the
      # account-deletion cascade. Each check asserts its SQLSTATE and constraint/message.
      # @spec [Doc-04A_V2.2, §5, §6, §9.2, §11.2; Doc-04B_V4.3, §13] | @implemented [2026-09-24]
      - name: Exam runtime schema gates (04A §5/§6/§9 constraints observed firing)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/exam-runtime-schema-gates.sh

      # Doc 04B V4.3 §9-§12 (E4) — every scoring-engine constraint is made to FIRE on
      # a fresh apply that ends with v1.0 ACTIVE: the activation row and its three
      # attestation fields, insert-once by each layer separately (privilege 42501,
      # trigger P0001, unique 23505, RLS 0 rows), the post-activation constants seal,
      # the NaN/Infinity/key-charset CHECKs, the version gate (candidate blocked,
      # superseded scores — §19.6), replay idempotency, the presented-item filter,
      # blanks-count-wrong, partial scoring, and the account-deletion cascade.
      # @spec [Doc-04B_V4.3, §9, §10, §11, §12, §19; SCL-128, SCL-129] | @implemented [2026-09-24]
      - name: Scoring engine gates (04B §9-§12 constraints observed firing)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/scoring-engine-gates.sh

      # Doc 04B V4.3 §18.2 / Appendix B + owner ruling (repo-hosted packet): the
      # committed packet hashes to 29c3e0fd… (= validation_packet_sha256 of v1.0),
      # its validation_sweep.py is byte-identical to docs/Spec, and it regenerates.
      # @spec [Doc-04B_V4.3, §18.2, Appendix B] | @implemented [2026-09-24]
      - name: Scoring evidence packet (hash + provenance)
        run: bash scripts/ci/scoring-evidence-packet-check.sh

      # Doc 04A V2.2 §4/§7-§16 + E6 rulings — the exam runtime functions the API calls,
      # driven on a fresh apply: RLS on every exam table AS `authenticated` (own rows
      # only, B's rows invisible, answer/routing columns not granted), server-time
      # expiry (409 module_submitted, section timed out), routing lock, idempotent
      # replay (one ledger row), module '2' -> locked path, strict break bounded /
      # lenient unbounded, lenient pause vs strict, inline finalisation of past-grace
      # sessions with a partial score, and the sweep. Each check prints its own `ok`.
      # @spec [Doc-04A_V2.2, §4, §7-§16; SCL-131..136] | @implemented [2026-09-24]
      - name: Exam runtime API gates (04A runtime behaviour + RLS, E6)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/exam-runtime-api-gates.sh

      # Doc 05E §5.1/§6 + Doc 05D §10 (E6b, SCL-143) — the exam tables in the
      # account-deletion cascade: whole exams walked through the runtime functions and
      # scored, then both modes. anonymize retains sessions, children, scores, ledger and
      # outbox with student_id NULL and actor_id intact; hard_delete removes and counts
      # every table; the INV-05E-07 sentinel refuses an ungrouped exam row; a pending or
      # in-progress exam of an anonymised student still scores (student_id NULL); a
      # re-run is a no-op; the insert-once trigger still refuses every caller mutation.
      # @spec [Doc-05E §1, §5.1, §6; Doc-05D §10.5; Doc-04B_V4.3 §9.4; SCL-143] | @implemented [2026-09-24]
      - name: Exam deletion cascade gates (both modes, E6b)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/exam-deletion-cascade-gates.sh

      # E7a (SCL-145..148) — the exam shell's server pieces, on a fresh apply: the item
      # workspace (served tokens only — a canonical letter, another item's token or a
      # repeat is refused; highlights are code-point offsets inside the passage; no notes;
      # active module and owner only), the heartbeat's resume position (read back only in
      # the module it names), GET /forms (drafts never listed, no thresholds), the 04C
      # report source (one 403 for missing and foreign sessions, the dead-letter failure
      # stand-in, the v1.0 disclosure seeded from 04B §17.1 verbatim) and grants.
      # @spec [Doc-04A_V2.2 §8.3, §15.1, §16; Doc-04C_V1.0 §5.3, §15.2, §16.6] | @implemented [2026-09-25]
      - name: Exam shell server gates (workspace, resume, forms, report source — E7a)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/exam-shell-server-gates.sh

  # ---------------------------------------------------------------------------
  # Practice engine integration gate (Doc-02B_V4 §14/§20). Seeds real published
  # questions into ephemeral Postgres, exercises select_practice_pool_random,
  # verifies non-zero selection, column reconstruction, anti-leak RPC output,
  # config doctrine (quota RPC rejects missing config / succeeds with config),
  # and idempotency. No mocks — real SQL against real schema.
  # ---------------------------------------------------------------------------
  # ---------------------------------------------------------------------------
  # Doc 05F calendar — parity gate.
  #
  # calendar_formula_reference.py is the oracle, as validation_sweep.py is for
  # Doc 04B. This runs the nine committed fixtures AND the seeded suite,
  # regenerated from rand_snapshot and never stored, through both the reference
  # and the PL/pgSQL RPCs, and fails on any byte difference or suite violation.
  #
  # WHY 17 AND NOT 16. genesis-fresh-apply above runs on 16 and that is fine —
  # it proves the pipeline applies and matches the committed snapshot. This gate
  # proves the FORMULA, and the two things most likely to differ between majors
  # are exactly what it is built to catch: security_invoker semantics and
  # integer-division edges. Prod is 17, so the formula is proved on 17
  # (Doc 05F formula sheet §6). CALENDAR_PARITY_REQUIRE_PG_MAJOR makes a
  # mis-pinned service container fail loudly rather than quietly prove less.
  #
  # The gate also refuses to exit 0 having compared fewer plans than it claims
  # to run — a parity gate that silently compares nothing is the same vacuous
  # pass this workflow guards against elsewhere.
  # @spec [Doc_05F_formula_sheet.md §6 "Parity gate"; INV-08-06 as amended by
  #        sheet §8 item 7] | @implemented [2026-09-17]
  # ---------------------------------------------------------------------------
  calendar-parity:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg17   # prod major; genesis declares the `vector` extension
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Apply the migration pipeline to a throwaway database
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          psql -v ON_ERROR_STOP=1 -d postgres -c "CREATE DATABASE calendar_parity;" >/dev/null
          # Supabase-provided roles and auth schema; a plain Postgres has neither.
          psql -v ON_ERROR_STOP=1 -d calendar_parity -q >/dev/null <<'SQL'
          DO $$ BEGIN
            IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname='anon')          THEN CREATE ROLE anon NOLOGIN; END IF;
            IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname='authenticated') THEN CREATE ROLE authenticated NOLOGIN; END IF;
            IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname='service_role')  THEN CREATE ROLE service_role NOLOGIN; END IF;
          END $$;
          CREATE SCHEMA IF NOT EXISTS auth;
          CREATE TABLE IF NOT EXISTS auth.users (id uuid PRIMARY KEY, email text, raw_user_meta_data jsonb);
          CREATE OR REPLACE FUNCTION auth.uid() RETURNS uuid LANGUAGE sql STABLE AS $f$ SELECT NULL::uuid $f$;
          SQL
          # Same sorted order scripts/ci/genesis-fresh-apply.sh uses, so the schema
          # under test is the one the pipeline produces.
          for f in supabase/migrations/*.sql; do
            psql -v ON_ERROR_STOP=1 -d calendar_parity -q -f "$f" >/dev/null
          done

      - name: Calendar schema + config gates
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          psql -v ON_ERROR_STOP=1 -d calendar_parity -f scripts/ci/calendar-schema-gates.sql
          # C-09's other half. The gate above proves generator_version LOOKS like
          # a migration timestamp; only the filesystem can say whether it names a
          # migration that exists, and a provenance stamp pointing at no file is
          # worse than none at all.
          gv="$(psql -qtAX -d calendar_parity -c "SELECT value #>> '{}' FROM public.calendar_runtime_config WHERE key = 'generator_version'")"
          if ! ls "supabase/migrations/${gv}"_*.sql >/dev/null 2>&1; then
            echo "CALENDAR_SCHEMA_GATE_FAILED: C-09 generator_version=${gv} names no migration under supabase/migrations/"
            exit 1
          fi
          echo "    OK C-09b generator_version ${gv} names $(ls supabase/migrations/${gv}_*.sql)"
          # C-09c. The stamp must name the NEWEST migration that redefines a generator
          # function -- not merely one that exists, and NOT the newest calendar migration.
          #
          # The distinction is the whole point of the stamp. Doc 05F §10.2 makes
          # generator_version the provenance on every calendar_plan_versions row, and the
          # seed's own comment names the five functions that matter: calendar_compute_plan,
          # calendar_compute_plan_fallback, calendar_place_full_lengths,
          # calendar_plan_to_output, calendar_validate_plan. A calendar migration that
          # changes RLS, a config row or calendar_build_plan_input does not change the plan
          # a given input produces, so bumping the stamp for it would make every stored
          # version claim a revision that never touched the formula.
          newest="$(grep -lE 'FUNCTION public\.(calendar_compute_plan|calendar_compute_plan_fallback|calendar_place_full_lengths|calendar_plan_to_output|calendar_validate_plan)\(' supabase/migrations/*.sql \
            | xargs -n1 basename | cut -d_ -f1 | sort | tail -1)"
          if [ "${gv}" != "${newest}" ]; then
            echo "CALENDAR_SCHEMA_GATE_FAILED: C-09c generator_version=${gv} but the newest migration redefining a generator function is ${newest}."
            echo "  A migration changed the formula without updating the provenance stamp, or the stamp was bumped for a migration that did not."
            exit 1
          fi
          echo "    OK C-09c generator_version ${gv} is the newest migration redefining a generator function"

      - name: Calendar grant + RLS gates
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: psql -v ON_ERROR_STOP=1 -d calendar_parity -f scripts/ci/calendar-rls-gates.sql

      - name: Calendar write-RPC gates
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: psql -v ON_ERROR_STOP=1 -d calendar_parity -f scripts/ci/calendar-writer-gates.sql

      # Real concurrent sessions, not a single-session script: the FOR UPDATE on
      # the study profile that makes version_no = MAX+1 safe (INV-08-17) is
      # invisible unless somebody else is holding it. This is also where the
      # idempotency ledger's check-then-insert is proved atomic.
      - name: Calendar concurrency gate (INV-08-17, INV-08-09)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: calendar_parity
        run: bash scripts/ci/calendar-concurrency-gate.sh

      # The launch seam: a calendar block becomes a practice session, and the filter
      # and the idempotency key have to arrive intact. Half A runs anywhere; half B
      # needs a real schema with real rows, which is why it lives in this job and is
      # skipped in the plain `ci` job like every other *.ci.test.ts.
      #
      # THROUGH THE SUMMARY GATE, for the reason the review step below spells out. This
      # step already had the environment and already passed, so nothing about it was
      # wrong -- but nothing would have SAID so if half B stopped running. `CAN_RUN` here
      # is `!!process.env.PGHOST`, so a renamed variable, a dropped service container or
      # a new PG-gated case added to the file all turn 13 passed into 8 passed and 5
      # skipped, with exit 0 either way. The gate reads vitest's own verdict and treats
      # `numPendingTests > 0` as red, so the difference between those two outcomes stops
      # being invisible. Same shape as the review and activity-units steps: its own
      # report file, named explicitly, so a failure says which contract broke.
      - name: Calendar practice launch contract (real PostgreSQL)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/calendar.launch-contract.practice.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-calendar-practice-contract.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-calendar-practice-contract.json \
            --file tests/ci/calendar.launch-contract.practice.ci.test.ts

      # REVIEW IS NO LONGER A STUB, AND THIS STEP HAD NOT NOTICED. It ran the review
      # contract with no PG environment, so `describe.skipIf(!PG_AVAILABLE)` skipped the
      # entire real-engine half -- 9 passed, 6 SKIPPED, exit 0, green from the outside.
      # Every claim the half exists to prove (the real session create, the replay, the
      # answered-vs-skipped predicate, the occurred_at window) was unproven in CI while
      # the step's own name still called review a fail-open stub.
      #
      # It now gets the same service container the practice contract above uses, and the
      # summary gate reads vitest's machine-readable verdict: `numPendingTests > 0` is
      # red, so a half that stops running can never again read as a pass. Separate
      # invocation and separate report from the activity-units file below, so a failure
      # names which claim broke.
      - name: Calendar review launch contract (REAL engine, real PostgreSQL)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/calendar.launch-contract.review.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-calendar-review-contract.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-calendar-review-contract.json \
            --file tests/ci/calendar.launch-contract.review.test.ts

      # The occurred_at ruling, both halves. Half A/B run anywhere (recording fake client,
      # including the plant: a resolved row with a NULL answered_at must still count); half C
      # needs real PostgreSQL, because "the CHECK guarantees occurred_at and nothing
      # guarantees answered_at" is a claim about the schema. Gated for the same reason as
      # above -- half C skipping would otherwise be invisible.
      - name: Calendar activity units — retrieval, and dated by occurred_at
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/calendar.activity-units.skip.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-calendar-activity-units.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-calendar-activity-units.json \
            --file tests/ci/calendar.activity-units.skip.ci.test.ts

      # Full-length IS still a fail-open stub, and its contract is the one its rebuilt
      # engine must pass unchanged. Its own invocation: a shared run would let a rebuild
      # quietly satisfy another engine's contract. No PG environment because the file has
      # no database half to run -- not because one is being skipped.
      - name: Calendar full-length launch contract (fail-open stub)
        run: pnpm exec vitest run tests/ci/calendar.launch-contract.full_length.test.ts

      # INV-08-18 through the REAL transport. The recorder-based launch tests prove the
      # sequence ARITHMETIC; only this proves that calendar_link_launch and the practice
      # engine's own idempotency ledger, reached over a wire as a real service_role JWT,
      # agree about what a repeated key means. The gate greps its own output because a
      # self-skipping vitest file exits 0 and reads green from the outside.
      - name: Install PostgREST (real transport)
        run: |
          curl -sL -o /tmp/pgrst.tar.xz \
            https://github.com/PostgREST/postgrest/releases/download/v12.2.3/postgrest-v12.2.3-linux-static-x64.tar.xz
          tar -xf /tmp/pgrst.tar.xz -C /tmp
          sudo mv /tmp/postgrest /usr/local/bin/postgrest
          postgrest --version

      - name: Calendar launch crash-retry → REAL PostgREST proof (INV-08-18)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/calendar-postgrest-gate.sh

      - name: Parity vs calendar_formula_reference.py (9 fixtures + seeded 3000 suite)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: calendar_parity
          CALENDAR_PARITY_REQUIRE_PG_MAJOR: "17"
        run: pnpm exec tsx scripts/ci/calendar-parity.ts --suite-n 3000 --suite-seed 1

  practice-integration:
    runs-on: ubuntu-latest
    # 20, not 10. Observed runs on this job: 5m14s, 10m04s (killed), 10m14s (killed),
    # 7m20s. The work fits; the margin did not, and GitHub reports a timeout kill as
    # "cancelled", which reads like an infrastructure hiccup rather than a budget.
    timeout-minutes: 20
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Practice engine integration gate (SQL seam only — no TS handler)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/practice-integration.sh

      # @spec [Codex re-audit Fix C] Handler-driven real-PG proof.
      # The bash test above proves the SQL seam; this vitest test proves the
      # TS handler actually calls applyMasteryEvent against real Postgres.
      # Would FAIL if `await applyMasteryEvent(...)` were removed from the handler.
      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Diagnostic handler → real PG LOGIC proof (mocked transport)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: pnpm exec vitest run tests/ci/diagnostic.handler-pg.ci.test.ts

      # @spec [Doc-01_V8 §20–§24 | STRIPE-001] Entitlement write-path → real PG proof.
      # Proves upsertEntitlement(profileId, ...) persists against the REAL genesis schema
      # (profile_id + UNIQUE(profile_id), NO account_id). Would FAIL against the old
      # account_id code (column does not exist). Also proves entitlement_active() and
      # the stripe_webhook_events idempotency gate against real PG.
      - name: Entitlement write-path → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: pnpm exec vitest run tests/ci/entitlement-write-path.ci.test.ts

      # @spec [owner instruction 2026-08-28 Task 1 Gates A and B] | @implemented [2026-09-01]
      #
      # The three files converted off mocked query layers and hand-written row fixtures on
      # 2026-09-01, added here by hand per the NOTE above — which is the whole reason that
      # note exists. Without this step they run in NO job: the whole-suite `pnpm test` step
      # does not set PGHOST, so `describe.skipIf(!PG_AVAILABLE)` skips every one of them and
      # reports green.
      #
      # WHY THE SKIP CHECK. A PG-backed suite that skips exits 0. That is a vacuous pass, and
      # a vacuous pass is exactly the defect class this job exists to stop — the same shape as
      # "zero scanned files is not zero violations" in the gates. If PGHOST ever stops
      # reaching the runner, this step must go RED rather than quietly stop proving anything.
      # @spec [SCL-080] | @implemented [2026-09-01]
      #
      # Added by hand per the NOTE above. Proves the code flow against a real database:
      # single-use consumption, the identical response for used/expired/never-real, and the
      # two-guardian race — the last of which no mocked query layer can establish, because
      # the thing under test is that POSTGRES settles it.
      #
      # The suite applies NO private DDL. Everything it needs comes from
      # supabase/migrations/ (20260901000000_scl_080_guardian_link_code.sql), applied in the
      # same sorted order scripts/ci/genesis-fresh-apply.sh uses, so the schema under test is
      # the schema the pipeline produces rather than one the test arranged for itself.
      - name: Guardian link code → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          node scripts/ci/vitest-summary-gate.mjs --selftest
          # R10 (2026-09-03): the verdict is vitest's JSON report, not a substring grep over
          # raw stdout — a log line containing "skipped" once turned a 7/7 pass red here.
          pnpm exec vitest run tests/ci/guardian-link-code.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-guardian-link-code.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-guardian-link-code.json \
            --file tests/ci/guardian-link-code.pg.ci.test.ts

      # @spec [Doc_05F_Study_Calendar §16] | @implemented [2026-09-22]
      # The guardian's way in to a student's calendar. Its student rows are INSERTED into
      # the throwaway Postgres above and READ BACK rather than spelled by hand, which is
      # what the guardian schema-truth gate (Rule B) requires — so it needs a database, and
      # the plain `ci` job has none. Behind the summary gate because a skipped file exits 0.
      - name: Guardian calendar link → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run client/src/pages/guardian-dashboard.calendar-link.pg.ci.test.tsx \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-guardian-calendar-link.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-guardian-calendar-link.json \
            --file client/src/pages/guardian-dashboard.calendar-link.pg.ci.test.tsx

      # @spec [contracts/notifications.contract.md §13] | @implemented [2026-09-03]
      # The notifications rebuild's proof: every DB-touching case runs against the real
      # migrations on the throwaway Postgres above, with no mocked table anywhere — the
      # deleted stack passed CI for months by mocking tables that existed in no environment.
      # Verdict read from vitest's JSON report (scripts/ci/vitest-summary-gate.mjs): a
      # PG-backed suite that skips is a vacuous pass, and a substring grep cannot tell.
      # 2026-09-15: the retention sweep (C11.1–C11.3: 90-day window defined once in SQL, the
      # cascade proven by message count, the zero-deletion log line) and the notifications
      # page API (archive view, mark-all-read, recipient denial) run in the same step.
      # @spec [Doc-03_V3 §21.2 step 5, §21.3; CR-03C-V3-01 §3.4; WS-L8 Item 4b;
      #        owner ruling 2026-09-22 D1] | @implemented [2026-09-22]
      # Flagging a conversation for crisis review used to be two writes in sequence, and
      # between them sat a state the system must never be in: a conversation MARKED as a
      # crisis with NOTHING in the review queue. Nothing sweeps for that, and the 48h SLA
      # never starts because the SLA lives on the case that was never created. Both writes
      # are now one transaction. D1.3 is the ruling's own test — force a failure after the
      # flag write, require that nothing survives — and it can only be shown on a real
      # database, because the whole claim is about transaction boundaries. Verdict read from
      # vitest's JSON report: a PG-backed suite that skips is a vacuous pass.
      - name: Crisis flag atomicity (Doc 03 §21.3 / D1) → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/crisis-flag-atomic.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-crisis-flag-atomic.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-crisis-flag-atomic.json \
            --file tests/ci/crisis-flag-atomic.pg.ci.test.ts

      # A gate is worth its red proof. Nine mutations, one per assertion; M1 re-plants the
      # PRE-D1 behaviour (the case insert's failure swallowed, the flag left standing) and
      # requires D1.3 to notice.
      - name: Crisis flag atomicity self-test (proves each mutation turns it red)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: bash scripts/ci/crisis-flag-atomic.mutations.sh

      - name: Notifications rebuild → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run \
            tests/ci/notifications.pg.ci.test.ts \
            tests/ci/notifications-retention-page.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-notifications.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-notifications.json \
            --file tests/ci/notifications.pg.ci.test.ts \
            --file tests/ci/notifications-retention-page.pg.ci.test.ts

      # @spec [Doc-01_V8 §36.3 (either party revokes; the other is told), §36.2 (invite limits);
      #        contracts/notifications.contract.md §2.3 guardian_unlinked row, §13] | @implemented [2026-09-15]
      # Guardian revoke → guardian_unlinked event (recipient = v_target only, revocation_reason
      # in no payload and no template) and the guardian INVITE direct send (idempotent key,
      # both §36.2 buckets on the real ledger, redeem still requires auth, byte-identical
      # response for known/unknown addresses). Real migrations on the throwaway Postgres;
      # verdict from vitest's JSON report so a skipped PG suite cannot read as a pass.
      - name: Guardian unlink event + invite → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run \
            tests/ci/guardian-unlinked.pg.ci.test.ts \
            tests/ci/guardian-invite.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-guardian-unlink-invite.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-guardian-unlink-invite.json \
            --file tests/ci/guardian-unlinked.pg.ci.test.ts \
            --file tests/ci/guardian-invite.pg.ci.test.ts

      # @spec [Doc-01_V8 §40.5; SCL-083 PROPOSED; contracts/notifications.contract.md C0.6] | @implemented [2026-09-15]
      # Deletion-completed notice: the address is read BEFORE the RPCs (call order), a failed or
      # rolled-back deletion sends nothing, a rejected send leaves the deletion committed and the
      # batch continues, the raw address is in no log line, a second cron pass sends nothing.
      # Real migrations on the throwaway Postgres; verdict from vitest's JSON report so a skipped
      # PG suite cannot read as a pass.
      - name: Deletion-completed notice → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/deletion-completed-notice.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-deletion-completed.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-deletion-completed.json \
            --file tests/ci/deletion-completed-notice.pg.ci.test.ts

      # @spec [SCL-085/086/088 PROPOSED; plan v4 §1 (two universes, three rules); owner brief
      #        2026-09-16 "Deletion Vertical" C3 tests 1-7 + B3] | @implemented [2026-09-16]
      # Evidence bundle: structural (no timestamp / uuid / outside-FK on the evidence side),
      # cross-universe xmin, rank (execution order ORDER BY profile_id — fails if the ORDER BY is
      # removed), guardian pre-clear in its own transaction, cancelled outcome, rolled-back cascade
      # reconciled both ways, no free text crosses, Stripe cancel arguments, Stripe failure →
      # failed_manual with the deletion still completing, billing record structural.
      - name: Deletion evidence bundle → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/deletion-evidence-bundle.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-deletion-evidence.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-deletion-evidence.json \
            --file tests/ci/deletion-evidence-bundle.pg.ci.test.ts

      # The proof above is only worth its red proof: plant one mutation per assertion (drop the
      # ORDER BY, share a transaction, add a timestamp column, disable the reconciler, cancel
      # with proration, ...) and require the NAMED test to turn red each time.
      # @spec [Doc-01_V8 §5 / §5.1 / App A.5 / App E; SCL-085 / SCL-087 (PROPOSED); SCL-090
      #        (PROPOSED as ruled 2026-09-17); owner brief 2026-09-17 "Deletion Vertical: Phases
      #        2, 3, 5" and its follow-up "Replace Bespoke Suppression With Resend's"]
      #        | @implemented [2026-09-17]
      # Phases 2/3/5: the audit_logs guard refuses UPDATE/DELETE for service_role and the single
      # exempt function is the only body that may open the gate; the three deletion actions are
      # written, with profile_hard_deleted proven to share no transaction with the pseudonymous
      # side; the completion notice reaches the provider BEFORE the suppression call, the call is
      # made only when it was asked for, a failure pages and is retried, and the subject can lift
      # it from account settings; the 24-month strip strips rather than deletes, leaves a 729-day
      # row alone, and says so when it changed nothing.
      - name: Deletion phases 2/3/5 → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/deletion-phases-235.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-deletion-phases.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-deletion-phases.json \
            --file tests/ci/deletion-phases-235.pg.ci.test.ts

      # @spec [Doc 03 §14.2; Doc 05E §3 Rule 4 + §5; Doc 01 V8 §40.5; owner brief 2026-09-17
      #        "Declarative FK Actions, Not an Enumerated Cascade" steps 3-4]
      #        | @implemented [2026-09-17]
      # The cascade used to enumerate table names by hand and nine foreign keys into `profiles`
      # were never added to it, so `DELETE FROM profiles` raised for anyone who had used the
      # tutor. The migration fixes those nine. THIS is what stops the tenth: the guard reads
      # `pg_constraint`, not a list, so a table added tomorrow is in scope the moment it has a
      # foreign key — whether or not anybody remembered the guard file. It also checks the
      # allowlist in both directions and refuses an edge handled both declaratively and
      # procedurally.
      - name: FK delete-action guard (catalog-driven, on a genesis fresh-apply)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/fk-delete-action-guard.sh

      # The behaviour behind the guard, on real Postgres: the seven tutor tables go with the
      # profile, a config row survives its attributor with a NULL, an activity row survives with
      # the identity severed and `actor_id` intact, and a brand-new unclassified foreign key
      # turns the guard red.
      - name: Declarative FK delete actions → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/deletion-fk-actions.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-deletion-fk.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-deletion-fk.json \
            --file tests/ci/deletion-fk-actions.pg.ci.test.ts

      # @spec [Doc 03 §21/§14.2 via owner ruling A6; Doc 06D §6.2/§6.5 INV-06-08 as amended by
      #        owner rulings A2/A4/B2/B3; owner brief 2026-09-17 "Deletion Vertical — Owner
      #        Rulings + Phase 6"] | @implemented [2026-09-18]
      # Phase 6: the last account-deletion blocker and the proof that a deletion happened.
      # A student whose tutor conversation was flagged for crisis review could not be deleted —
      # three crisis edges were still RESTRICT behind the tutor CASCADE. They are SET NULL now,
      # the case survives with both links severed, and the denormalized conversation_id copy is
      # severed by trigger so no deletion path can leave it behind. P6.6 sweeps every uuid
      # column in the schema to prove the verification record's dead-profile uuid is the only
      # place that uuid survives.
      - name: Deletion phase 6 (crisis severance + verification record) → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/deletion-phase-6.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-deletion-phase6.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-deletion-phase6.json \
            --file tests/ci/deletion-phase-6.pg.ci.test.ts

      # Privacy Policy v3 §6.7 published a 90-day ceiling on operational records that
      # name you and fall outside the ten enumerated categories. SCL-101 filed that
      # sentence as a BUILD COMMITMENT because nothing enforced it. This suite is the
      # enforcement: a row past its window goes, a row inside it stays, and a run that
      # deleted nothing still reports every table — the last one because Vercel's cron
      # registration cannot be read back from tooling, so an unscheduled sweep and an
      # empty sweep are otherwise indistinguishable.
      - name: Operational-log retention (v3 §6.7 catch-all) → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/operational-log-retention.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-oplog-retention.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-oplog-retention.json \
            --file tests/ci/operational-log-retention.pg.ci.test.ts

      # Doc 01 V8 §5.1 says "retention values live in observability_runtime_config".
      # The table has held nothing since genesis and NOTHING READS IT; the owner
      # ruling seeds it anyway so the declared period and the enforced period can be
      # compared. This suite IS that comparison — without it the rows are decoration
      # that drifts from behaviour the first time a period changes. F2.4 and F2.5 are
      # negatives: cold_log_retention_days and alert_thresholds must stay unseeded,
      # because neither the cold archive nor the alert registry exists.
      - name: Observability retention config (Doc 01A A.5 / F2) → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/observability-retention-config.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-obs-retention-config.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-obs-retention-config.json \
            --file tests/ci/observability-retention-config.pg.ci.test.ts

      # v3 §6.2 publishes a seven-year period for payment records. It will delete
      # nothing until 2033, which is exactly why its behaviour is pinned now: a
      # sweep with an empty input for seven years is one nobody would notice was
      # broken. B2.9 additionally pins the append-only blocker that kept §6.5's
      # configuration sweep out of the same migration.
      - name: Seven-year payment-record retention (v3 §6.2) → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/financial-record-retention.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-financial-retention.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-financial-retention.json \
            --file tests/ci/financial-record-retention.pg.ci.test.ts

      # The writer that did not exist. retention-sweep.ts:113 documented
      # tutor_conversations.deleted_at as "set when entitlement lapses" from
      # 2026-08-20; nothing set it, so sweep7d's input was permanently empty and
      # Privacy Policy v3 had to publish the narrower account-deletion wording.
      # C1.6 closes the loop by showing the sweep's own predicate matching a row.
      # C1.7 guards the coupling between the trigger's column list and the
      # canonical predicate's body — the way this would rot silently.
      - name: Tutor lapse severance (Doc 03 §14.2 / C1) → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/tutor-lapse-severance.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-tutor-lapse.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-tutor-lapse.json \
            --file tests/ci/tutor-lapse-severance.pg.ci.test.ts

      - name: Deletion evidence bundle → mutation self-test (every gate observed red once)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: bash scripts/ci/deletion-evidence-gate.mutations.sh

      - name: Guardian schema-truth conversions → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run \
            tests/ci/guardian-checkout.contract.test.ts \
            tests/ci/guardian-premium-fold.contract.test.ts \
            packages/shared/src/__tests__/guardian-student-schema.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-guardian-conversions.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-guardian-conversions.json \
            --file tests/ci/guardian-checkout.contract.test.ts \
            --file tests/ci/guardian-premium-fold.contract.test.ts \
            --file packages/shared/src/__tests__/guardian-student-schema.test.ts

      # @spec [brief R3 §4; ruled plan §2] Review API → real PG proof.
      # Named BY FILE here on purpose: the whole-suite `pnpm test` step in the `ci` job
      # sets no PGHOST, so `describe.skipIf(!PG_AVAILABLE)` skips both files and exits 0.
      # A PG test not named in a service-backed job runs nowhere and reports green.
      - name: Review API + queue end-to-end → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run \
            tests/ci/review.routes.pg.ci.test.ts \
            tests/ci/review.queue-e2e.pg.ci.test.ts \
            --reporter=default --reporter=json --outputFile=/tmp/vitest-review.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-review.json \
            --file tests/ci/review.routes.pg.ci.test.ts \
            --file tests/ci/review.queue-e2e.pg.ci.test.ts

      # @spec [Doc-04A_V2.2 §7-§16; E6] Exam runtime API -> real PG proof: whole exams
      # through the real /api/tests handlers (both routing paths, both modes), answers by
      # SERVED TOKEN stored as the canonical letter, pre-submit payloads anti-leak, inline
      # scoring. Named BY FILE: the `ci` job sets no PGHOST, so this file skips there.
      - name: Exam runtime API → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/exam-runtime.handler-pg.ci.test.ts \
            --reporter=verbose --reporter=json --outputFile=/tmp/vitest-exam-runtime.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-exam-runtime.json \
            --file tests/ci/exam-runtime.handler-pg.ci.test.ts

      # @spec [Doc-05E §5.1; Doc-05D §10.5; E6b / SCL-143] Full lifecycle: one exam per
      # student through the real /api/tests handlers to a score, then the executor's
      # anonymize call and the hard_delete cascade; row counts before/after and the
      # result JSON are printed. Named BY FILE: the `ci` job sets no PGHOST.
      - name: Exam deletion cascade lifecycle → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/exam-deletion-cascade.handler-pg.ci.test.ts \
            --reporter=verbose --reporter=json --outputFile=/tmp/vitest-exam-deletion.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-exam-deletion.json \
            --file tests/ci/exam-deletion-cascade.handler-pg.ci.test.ts

      # @spec [Doc-04A_V2.2 §16 (SCL-145/145/146); Doc-04C_V1.0 §11.2, §16.1-§16.8; E7a]
      # The shell's server surface through the real /api/tests routers: forms list,
      # workspace round-trip with served tokens (a letter refused), heartbeat position,
      # and the 04C report in every reachable state plus the bare 403. Named BY FILE.
      - name: Exam shell server endpoints → real PG proof
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          pnpm exec vitest run tests/ci/exam-shell-server.handler-pg.ci.test.ts \
            --reporter=verbose --reporter=json --outputFile=/tmp/vitest-exam-shell.json
          node scripts/ci/vitest-summary-gate.mjs --json /tmp/vitest-exam-shell.json \
            --file tests/ci/exam-shell-server.handler-pg.ci.test.ts

      # The fourteen assertions above are worth exactly as much as their plants. This
      # mutates product source once per assertion and fails the build unless that
      # assertion goes red. It patches the working tree and restores it in a trap, so
      # it runs after everything that reads those files.
      - name: Review route assertions proven by planted violations (A1-A14)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: bash scripts/ci/review-routes-gate.mutations.sh

      # @spec [R2 (PR #795) shipped these gates; R3 wires them in.]
      # scripts/ci/review-queue-gates.sql landed with the R2 migration and was named in
      # no workflow, so nothing ran it. It is the only cover for the review reveal
      # matrix, the cross-student RLS policies and the EXECUTE grants on the two queue
      # functions — none of which the vitest suites above can see, because they connect
      # as the superuser.
      - name: Review queue DB gates (reveal matrix, RLS, EXECUTE grants)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: bash scripts/ci/review-queue-gates.self-test.sh

      # @spec [owner instruction 2026-08-27 Part B — "make the guards structural, not habitual"]
      # The proof above can only be trusted if its cases can FAIL. Three times in three
      # consecutive steps a case read green while proving nothing — a mutation that never
      # applied, a mutation that reddened an earlier assertion than the one it named, and a
      # precondition that put a different mechanism in the path. Each was caught by hand,
      # which is not a mechanism. This runs the declared mutations and fails the build unless
      # each one turns EXACTLY the cases it claims. It is the LAST step in this job because it
      # patches source files in the working tree and restores them in a finally.

  # ---------------------------------------------------------------------------
  # Mastery pipeline gates — HARD GATE.
  #
  # Three things nothing else covers:
  #   1. the unblock migrations, red-before/green-after against a seeded pre-state
  #   2. derivation-gap detection (the invariant: every derivable event has exactly
  #      one attributable audit row)
  #   3. the REAL transport — supabase-js -> PostgREST -> service_role JWT -> GRANT.
  #      Every other mastery test mocks the Supabase client; all of them were green
  #      throughout the outage.
  # ---------------------------------------------------------------------------
  mastery-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16   # genesis declares the `vector` extension
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      PGHOST: localhost
      PGPORT: "5432"
      PGUSER: postgres
      PGPASSWORD: postgres
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Red-before/green-after on a seeded pre-state, plus the portability case that
      # regression-tests the hardcoded-count blocker (a migration carrying a prod row
      # count reds every fresh apply).
      - name: Mastery unblock migration gates (1.1 + 1.2)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/mastery-unblock-gates.sh

      # Every file under scripts/prod-verify/ is pasted by hand into the Supabase
      # SQL editor and run against production — it is NOT run with `psql -f`.
      # This executes each one the way the operator does. Every prod-verify file
      # shipped in #589..#595 failed at its first backslash meta-command and CI
      # never noticed, because CI only ever ran the CI fixtures.
      - name: prod-verify console gate (operator SQL must run in a SQL console)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/prod-verify-console-gate.sh

      # captureDiagnosticBaseline is once-only with no retry and silently returns
      # when the projection is not ready, so a student who completed a diagnostic
      # during the mastery outage is permanently no_baseline. This guards the
      # repair — and above all that it stays a repair, never a fabricated score.
      # One-off exact-target resolution of the single duplicate diagnostic in prod
      # (ruling Q8). The fixture seeds a SECOND student's active diagnostic, which
      # is what makes it discriminate a pinned id from its superset.
      - name: Resolve-duplicate-diagnostic gate (Q8 — exact target)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/resolve-duplicate-diagnostic-gate.sh

      - name: Baseline repair gate (BUG-1 — repair, not fabrication)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/baseline-repair-gate.sh

      # Guards scripts/prod-verify/step8-verify.sql, which previously asserted
      # student_projection_refresh_state > 0 and so reported STOP on a completely
      # successful production backfill. Runs a REAL backfill and checks the real
      # file's verdict against it.
      - name: Step 8 backfill acceptance gate
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/step8-backfill-acceptance-gates.sh

      # Guards the two read-only files that decide whether a history repair is safe
      # (ruling Q9 puts the repair itself in the Supabase CLI's hands). Parity runs
      # FIRST and must be strict: it is the only thing between a hand-applied schema
      # and an irreversible "recorded as applied".
      # The detector reported 84 gaps out of 91 answered items on its first day in
      # production — every item the Step 8 backfill had correctly rebuilt, because
      # backfill_recompute_student writes no per-event audit row. An alert that is
      # 100% noise on arrival gets muted, which is the failure this workstream
      # exists to prevent. N4 reproduces the 84 before asserting the 0.
      - name: Gap-detector noise gate (backfill is not a gap)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/gap-detector-noise-gate.sh

      - name: Migration history gate
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/migration-history-gate.sh

      - name: Migration inventory gate (applied vs pending discrimination)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/migration-inventory-gate.sh

      # Session lifecycle, steps 2/1/8/9. Seven fixture students; the two that
      # earn their place are E (two ALL-NULL diagnostic_baseline rows — what the
      # evidence gate actually writes, so counting snapshot rows calls them
      # "ready") and G (a completed AND an active diagnostic at once, which is
      # production's real shape and the only way to prove the CASE precedence).
      - name: Session lifecycle DB gate (once-only index, state, abandoned_at)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/session-lifecycle-db-gate.sh

      - name: Mastery derivation gap detection gate (2.4)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          source scripts/ci/lib/deletion-rehearsal-db.sh
          setup_genesis_db mastery_gap_ci
          psql -v ON_ERROR_STOP=1 -d mastery_gap_ci -q -f scripts/ci/mastery-gap-detection-gates.sql
        shell: bash

      # Six level names + the drill-down catalog. The catalog replaces SAT_TAXONOMY,
      # whose invented slugs (math/advanced_math/linear_equations) never matched the
      # canonical DB values (M/Advanced Math/Linear Equations in One Variable), so the
      # skill tree resolved every node to NULL for every student.
      - name: Mastery levels + skill catalog gates (RULE 1/2/3/5)
        run: bash scripts/ci/mastery-levels-gate.sh
        shell: bash

      # The 2026-06-26 outage, reproduced as a permanent test: a corrupt row in one
      # section must not abort a valid mastery write in another. SCL-054 / INV-05B-16.
      - name: KPI quarantine gate (blast radius; excluded_event_count)
        run: bash scripts/ci/05b-kpi-quarantine-gate.sh
        shell: bash

      # Proves the gate above can fail, and fails on the assertion each mutation names.
      # M3 is the load-bearing one: without it "counted" and "excluded" are the same test.
      - name: KPI quarantine gate — mutation proof
        run: bash scripts/ci/05b-kpi-quarantine-gate.mutations.sh
        shell: bash

      # The gate that would have caught this outage on day one.
      - name: Install PostgREST (real transport)
        run: |
          curl -sL -o /tmp/pgrst.tar.xz \
            https://github.com/PostgREST/postgrest/releases/download/v12.2.3/postgrest-v12.2.3-linux-static-x64.tar.xz
          tar -xf /tmp/pgrst.tar.xz -C /tmp
          sudo mv /tmp/postgrest /usr/local/bin/postgrest
          postgrest --version

      # Runs tests/ci/mastery-emission.postgrest.ci.test.ts BY NAME. The gate greps
      # its own output for the EXECUTING marker and the exact case count, because a
      # self-skipping vitest file exits 0 and reads green from the outside.
      - name: Mastery emission → REAL PostgREST proof (route → supabase-js → PostgREST → PG)
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/mastery-postgrest-gate.sh

      # Proves the proof can fail, and that each mutation reds the assertion it names.
      # M1 and M3 both attack case 1 and are kept apart by WHICH line they red.
      - name: Mastery emission → REAL PostgREST proof — mutation harness
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/mastery-postgrest-gate.mutations.sh

  # ---------------------------------------------------------------------------
  # Destructive-path rehearsal (Doc 01 §40.5) — HARD GATE. Applies genesis + the
  # staged (owner-run) deletion-lifecycle migration to a throwaway Postgres and
  # proves the IRREVERSIBLE deidentify_user RPC + cron eligibility filter on the
  # real schema: exact-target (only the eligible row changes, others byte-identical),
  # plus idempotency / re-entrancy. A mocked unit suite cannot prove a WHERE clause.
  # ---------------------------------------------------------------------------
  deletion-deidentify-rehearsal:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16   # genesis declares the `vector` extension
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: deidentify_user exact-target + idempotency rehearsal
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/deletion-deidentify-rehearsal.sh

      - name: Negative control — rehearsal must FAIL on a broken WHERE
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/deletion-deidentify-negative-control.sh

      - name: In-app cancel atomicity (no-strand) rehearsal
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/deletion-cancel-atomicity.sh

      - name: Flag-on lifecycle e2e (request -> cron -> anonymize; recover/cancel -> restored)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/deletion-lifecycle-e2e.sh

  # ---------------------------------------------------------------------------
  # Mastery PARITY gate (Doc 05A §12 / §12.4a / §12.5) — HARD GATE. Proves the
  # PL/pgSQL compute_mastery_for_entity matches the independent Python reference
  # AND the §12 published values across all 31 fixtures, plus recompute-equivalence.
  # ---------------------------------------------------------------------------
  mastery-parity:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16   # genesis declares the `vector` extension
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Mastery formula parity (PL/pgSQL == Python reference == Doc 05A §12)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/mastery-parity.sh

  # ---------------------------------------------------------------------------
  # Scoring PARITY gate (Doc 04B V4.3 §6 / §18.3 / §21.1 / §28) — HARD GATE, run on
  # every CI run by OWNER RULING 2026-09-24, overriding §21.2 (which makes parity a
  # one-time lock-time check). Drives all 1,313 sweep scenarios + 60 targeted
  # fixtures of the committed evidence packet through the pure formula function AND
  # through real sessions scored by score_test_session_from_outbox(), plus every §28
  # worked example; any disagreement with the Python reference fails, printing the
  # first mismatches. COVERAGE LIMIT: this sees a repo-built database only — a
  # production edit made outside migrations is NOT covered (owner deferred a
  # prod-side drift check).
  # @spec [Doc-04B_V4.3, §3.1, §6, §18.3, §21.1, §28] | @implemented [2026-09-24]
  # ---------------------------------------------------------------------------
  scoring-parity:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16   # genesis declares the `vector` extension
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Scoring formula parity (PL/pgSQL == validation_sweep.py, formula + session paths)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/scoring-parity.sh

  # ---------------------------------------------------------------------------
  # Lane-C seam gates (Doc 05A §4) — HARD GATE. Proves the live event→mastery seam:
  # production-derivation parity (compute over the REAL canonical_mastery_events ==
  # Python reference == §12), idempotency replay, and transaction atomicity.
  # ---------------------------------------------------------------------------
  lane-c-seam-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Lane-C seam gates (production parity + idempotency replay + txn atomicity + F1/F2/F3 §4.9 re-proof)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/lane-c-seam-gates.sh

  # ---------------------------------------------------------------------------
  # 05B/05C rollup gates (Doc 05B §4-§10, Doc 05C §4-§8) — HARD GATES. Domain-mastery
  # parity (event-aggregation at domain grain, INV-05B-13) + KPI struct; State-A section
  # projection parity vs Doc 05C §6 worked examples (Q4 honest-gate + determinism).
  # ---------------------------------------------------------------------------
  rollup-05b-05c-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: 05B domain mastery + KPI gates (domain-grain parity + INV-05B-13 + KPI struct)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/05b-domain-kpi-gates.sh

      - name: 05C section projection gates (State-A projection parity vs Doc 05C §6 + Q4 gate)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/05c-projection-gates.sh

      - name: Guardian-mirror gates (airtight link + entitlement + view-only + carve-outs)
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/guardian-mirror-gates.sh

  # ---------------------------------------------------------------------------
  # Tutor schema-proof gate (AUD-519-008 closure — Doc 03A §18.1–§18.7). Proves
  # the tutor schema matches spec: required columns, types, enum values, RLS
  # enabled, and correct student-scoped policies across all 9 tutor_* tables.
  # Includes negative control: drops a required column and asserts the suite FAILS
  # — a gate never observed failing is not known to work.
  # ---------------------------------------------------------------------------
  tutor-schema-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Ensure postgresql-client 16
        uses: ./.github/actions/ensure-psql16

      - name: Tutor schema-proof gate
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/tutor-schema-proof.sh

      - name: Negative control — schema-proof must FAIL on missing column
        env:
          PGHOST: localhost
          PGPORT: "5432"
          PGUSER: postgres
          PGPASSWORD: postgres
        run: |
          export PATH=/usr/lib/postgresql/16/bin:$PATH
          bash scripts/ci/tutor-schema-proof-negative-control.sh

  # ---------------------------------------------------------------------------
  # GCP Workload Identity Federation auth gate. Authenticates via WIF and runs
  # a single gcloud command to prove the credential works. This job carries
  # `id-token: write` because the WIF exchange needs to mint a GitHub OIDC
  # token — no other job needs this permission. Once proven, the Vertex model
  # availability probe and other GCP-dependent CI steps can build on this job.
  # ---------------------------------------------------------------------------
  gcp-wif-auth:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP via Workload Identity Federation
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: projects/480325859530/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: lyceon-ci-eval@replit-cop.iam.gserviceaccount.com

      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@v3

      - name: Verify GCP auth (one command proof)
        run: gcloud auth list
