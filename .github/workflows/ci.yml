name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CI: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Run lint only in workspaces that define it
      - name: Lint workspaces
        run: pnpm -r run lint --if-present

      # Run typecheck only in workspaces that define it
      - name: Typecheck workspaces
        run: pnpm -r run typecheck --if-present

      # Build only workspaces that define build
      - name: Build workspaces
        run: pnpm -r run build --if-present
